kind: Deployment

image:
  repository: pet-node
  tag: "v1.2.0"

port: 8080

service:
  enabled: false

ingress:
  enabled: false

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 8
  targetCPUUtilizationPercentage: 80

global:
  security:
    allowInsecureImages: true

# Because you have to pay for the image that comes with the chart :(
image:
  registry: docker.io
  repository: bitnamilegacy/rabbitmq
  tag: 4.1.3-debian-12-r1
  pullPolicy: IfNotPresent

auth:
  username: user
  password: password

ingress:
  enabled: true
  hostname: rabbitmq.minikube.local